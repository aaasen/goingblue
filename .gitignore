# Node
node_modules/

# Build output
packages/*/dist/
*.tsbuildinfo

# Benchmark data: cached Open-Meteo responses (data/raw) and HTML reports (data/benchmarks)
data/

# Full-resolution photo originals. Only the resized, EXIF-stripped derivatives under
# packages/server/public are tracked and deployed; keep the camera files out of the repo.
/*.jpg
/*.jpeg

# macOS
.DS_Store

TODO.md

# Private deployment notes
DEPLOYMENT.md
A2P\ 10DLC.md

.claude

# API keys (node --env-file)
.env
